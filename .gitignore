.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/
.env
data/

# Benchmark PDF fixtures: regenerated by bench/make_pdf.py (fixed seed, so
# deterministic). large.pdf alone is 101 MB, over GitHub's 100 MB file limit -
# a push with it staged is rejected outright, not just slow.
bench/*.pdf
# Benchmark run artifacts: output of running bench/benchmark.py, not source.
bench/*.log
bench/result.json
